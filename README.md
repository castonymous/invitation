# Vintation-style Wedding Invite — Vite + React + Tailwind

This is a one-page wedding invite clone (mobile-first) ready to host on GitHub Pages / Netlify / Vercel.

## Quick start
1. Install dependencies: `npm install`
2. Run dev: `npm run dev`
3. Build: `npm run build`

Replace images in `src/assets/` with your own assets (hero, gallery, music).